# PROJECT METADATA (Local Dev Only)
# Note: For production, name and version are pulled from pyproject.toml
PROJECT_NAME=fastapi_setup

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=project_db
DB_HOST=localhost
DB_PORT=5432

# pgAdmin Credentials (Local Dev Only)
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-key-EdPSLpoxDwHVVci0
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Configuration
BACKEND_CORS_ORIGINS='["http://localhost:3000", "http://localhost:5173", "http://127.0.0.1:5173"]'

# Email Configuration
MAIL_HOST=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_PORT=587
MAIL_FROM=dev@test.com
